# Handover — AI Writing Suite: v1.1 shipped (P0→P2.5, 6 PRs) — 2026-06-10

Project: **AI Writing Suite** — `~/Documents/ai-writing-suite/`
GitHub: **github.com/surahli123/ai-writing-suite** (public, default `main`)
Branch: `main` @ `b2db0e5`. Working tree clean (only untracked docs/notes, blocked from commit by repo hooks). **65 tests green on merged main** (verified post-merge).

## Session goal (user-reset)

"Make ai-writing-suite really do the job." Company-laptop (RovoDev) testing surfaced two symptoms:
(1) sub-skills can't load knowledge, (2) initial drafts still sound AI-generated. This session diagnosed
root causes, ran deep research on what a "good" AI writing tool produces, got the v2 plan approved, and
shipped P0→P2.5.

## What shipped (6 PRs, all merged, CI green on every one)

| PR | What |
|---|---|
| #7, #8 | Prior session leftovers (RovoDev doc corrections + calibration table) — merged at session start |
| **#9 (P0)** | Suite-root path protocol: all `../../_shared/` refs → root-relative + "Locating shared assets" section in comms-polish/voice-onboard; guard test bans parent-relative `_shared` refs. Fixes RovoDev cwd-resolution breakage (symptom 1, path face) |
| **#10 (P1)** | **comms-draft implemented** (13-line placeholder → 207-line skill): per-task acceptance criteria (5 dims), draft-time KB+voice injection, burstiness, vary/roughen pass, post-draft self-scan, `[NEEDS:]` no-fabrication. `ReferencedSharedFilesExist` CI test. Demo: naive draft 20/100 (8 tells) vs skill draft 4/100 HUMAN_ONLY (1 tell) |
| **#11 (P2)** | **comms-qa implemented** (placeholder → 162-line skill): wraps INDEX.md retrieval protocol, KB-only cited answers, labeled "Outside the playbook:" separation, zero-match/missing-KB honesty. 2 question-shaped smoke cases (3→5) |
| **#12 (P2.5)** | **KB wiki-ized**: bidirectional `## Related entries` footers on all 5 entries (INDEX.md FROZEN — zero edits), comms-qa one-hop follow, `kb_lint.py` fork-contract checker (4 checks) + `test_kb_wiki.py` CI enforcement. Negative-tested: broken KB copy → 3/4 FAIL, exit 1 |

Tests grew 51 → **65** across the session; KB smoke 3 → 5 cases; fixtures 8/8 @ 38% calibration unchanged throughout.

## Key artifacts written this session (all untracked by design — repo hooks block them)

- `notes/diagnosis-kb-loading-rovodev-2026-06-09.md` — root-cause diagnosis (F1 KB unwired in v1, F2 `../../` path fragility, F3 comms-draft didn't exist)
- `notes/research-good-ai-writing-outcome-2026-06-09.md` — deep-research report: 14 findings (good ≠ detector evasion; close 5 gaps: concreteness/nuance/burstiness/lived emotion/lexical originality; blind evals; instance-specific criteria 84% vs 58-67%; judge needs human calibration; don't over-polish). Telemetry: E=0.64 (expensive run), 21 confirmed / 4 killed claims
- `docs/plan-v2-make-it-really-work-2026-06-09.md` — the approved plan (P0→P3)
- `docs/report-research-and-v2-decision-2026-06-09.html` — decision report (opened in browser)

## Key decisions / constraints (carry forward)

- **Detector score = regression signal, NEVER quality KPI** (research findings 12/14; broken-ruler). P1's planned "new eval fixtures" were deliberately deferred to P3 for this reason — owner accepted.
- **INDEX.md retrieval semantics frozen** — entry edits append-only; smoke cases hand-compute token overlap BEFORE writing; never edit INDEX/KB to make a test pass.
- **Wiki = structure, not tools**: pure-markdown Related-entries links (D5 portability stands, RovoDev = most constrained host). `kb_lint.py` protects the fork add-an-entry contract: file + INDEX row + bidirectional links + lint pass.
- **OMC subagents are BROKEN on this machine**: `~/.claude/agents/` empty since May 20; OMC 4.14.6 plugin dir has no agent defs; `oh-my-claudecode:executor` → "agent type not found". **Owner-approved fallback: native `claude` subagent type with the same tight spec** (scope files + verbatim text + verify commands + DO-NOT list + caveman-ultra footer). Worked 3/3 with zero out-of-scope touches.
- Repo hooks block direct `main` commits + committing `handover-*`/`plan-*` files. Branch → PR → rebase-merge (`gh pr merge --rebase`).
- Session cadence that worked: dispatch agent → independently re-verify (never trust agent self-report) → behavioral demo → stage → show staged files + message → owner OK → commit/PR/CI/merge.

## NEXT (ordered)

1. **Owner acceptance test on the company laptop**: re-copy the updated suite (`git clone` or copy `skills/ai-writing-suite/` over the old install), then explicitly invoke `comms-draft` with a real brief and `comms-qa` with a playbook question. Check: the "Inputs note" output line says what KB/voice was loaded; drafts read human. Also record the 3 pending confirmation points (which sub-skill failed before / error vs silent / was the playbook in `_shared/knowledge/`).
2. **P3 — eval upgrade (STILL BLOCKED)** on owner collecting ~16-24 real AI-shaped drafts on the enterprise computer. Then: `evals/fixtures/calibration.py` to size the set (n=24 → 8 misses), blind-eval protocol into rubric, instance-specific criteria, judge calibrated vs human labels FIRST. Never wire the judge into CI.
3. **Parked: AI slop / dead-code audit** (the session's original ask, superseded by the user's purpose reset). Now safe to schedule — v2 file churn is done.
4. Optional: version bump 1.0.0 → 1.1.0 in both plugin.json files + tag, once owner's acceptance test passes (per `docs/packaging.md` release steps).

## Handover prompt (paste into a fresh session)

> Continue **AI Writing Suite** at `~/Documents/ai-writing-suite/` (public repo
> github.com/surahli123/ai-writing-suite, `main` @ `b2db0e5`, 65 tests green). Read
> `docs/handover-2026-06-10-v1.1-p0-p2.5-shipped.md` first. Last session shipped v1.1:
> P0 suite-root paths (PR #9), P1 comms-draft (PR #10), P2 comms-qa (PR #11), P2.5 KB wiki +
> kb_lint (PR #12) — all four sub-skills now functional. I will report my company-laptop
> acceptance test results (comms-draft + comms-qa on RovoDev). Depending on results: fix what
> failed, or proceed to P3 eval upgrade (blocked on my real-draft collection — use
> `evals/fixtures/calibration.py` to size), or run the parked AI-slop/dead-code audit, or bump
> version to 1.1.0 + tag. Repo hooks: never commit to main; handover/plan files stay untracked.
> OMC subagents broken on this machine — use native `claude` agent dispatch with tight specs.
> Detector scores are regression signals, never quality KPIs. Plan first, wait for my go.
