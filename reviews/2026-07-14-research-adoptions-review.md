# Review record — research-adoptions batch (A1–A3 + B5), 2026-07-14

Chain: orchestrator line review (60%+ Liang stat traced to pre-existing :101; zero issues)
→ code-reviewer (isolated worktree): APPROVE — loader ground-truth 72, H2 gate invisible to
_CATALOG_HEADER, advisory coherence clean, D4 absent; 1 LOW (gate section locality, waived)
→ Codex gpt-5.6-sol xhigh solo fact-check: REVISE — 2 BLOCKERs, both arbitrated DOWN:

- B1 (S10 Fix imperatives = single-signal mandate): OVERSTATED — Fix: is catalog-wide schema
  ('what to do when acting', not 'whether'), S10 carries an explicit advisory/judge-only
  validity condition; hardened anyway: Fix now opens 'applies only once the validity
  condition holds and the cluster rule warrants acting'.
- B2 (step-5 'remove the tells' + all-no gate = single-signal edit permission violating the
  ruling): REFUTED on scope — Q16's advisory constraint covers provenance-only signals;
  quality-degrading tells are the product's core edit target, and 'remove the tells you
  find' is pre-existing text. Hardened anyway: wiring now states an all-no gate is not a
  mandate; cluster guardrail (signals, not proof) still applies.
- CONCERN (wiring summary narrower than gate: 'quote/code' vs quote/citation/code/command/
  path): CONFIRMED — summary enumeration completed.

Codex catalog-integrity probes all passed (10 S-headings; 00-index 72; pin 72; H2 no phantom
entry; no agent-audience feature). Post-hardening verification rerun green.

VERIFIED_AGAINST: feat/research-adoptions @ post-hardening HEAD
